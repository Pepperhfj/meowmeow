button.onclick = increaseValue; // Clicking the button will activate the increaseValue function !

function increaseValue() {
  var value = document.getElementById('meow'); // Get the ID of 'meow' and assigns it as a variable >_<
  value.innerHTML +=" meow !"; // Increases the 'value' amount in the HTML ^o^
}
